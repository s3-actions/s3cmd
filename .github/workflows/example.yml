on: [push]

jobs:
  hello_world_job:
    
    runs-on: ubuntu-latest
    
    name: expose s3cmd
    steps:
    - name: s3cmd
      id: s3cmd
      uses: s3-actions/s3cmd@main
    - name: Get the output time
      run: echo "The time was ${{ steps.s3cmd.outputs.stdout }}"
